# Flu prod forecasters are now defined as grid rows in scripts/flu_hosp_prod.R
# (id, bare forecaster function, params). Per-forecaster wrapping lives in
# run_prod_forecaster() (R/targets/prod_forecaster_runner.R). This file is
# retained only as a pointer and will be removed once the migration is complete.
