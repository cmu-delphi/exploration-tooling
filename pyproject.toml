[project]
name = "exploration-tooling"
version = "0.1.0"
description = "Python tooling for NWSS export and one-off analyses"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.3.3",
]
