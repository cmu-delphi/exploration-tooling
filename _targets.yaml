covid_hosp_explore:
  script: scripts/covid_hosp_explore.R
  store: covid_hosp_explore
  use_crew: yes
  reporter_make: timestamp
flu_hosp_explore:
  script: scripts/flu_hosp_explore.R
  store: flu_hosp_explore
  use_crew: yes
  reporter_make: timestamp
flu_hosp_prod:
  script: scripts/flu_hosp_prod.R
  store: flu_hosp_prod
  use_crew: yes
  reporter_make: timestamp
flu_hosp_evaluation:
  # Same script as flu_hosp_prod; it dispatches on the project name. Separate
  # store so evaluation runs don't invalidate the production cache.
  script: scripts/flu_hosp_prod.R
  store: flu_hosp_evaluation
  use_crew: yes
  reporter_make: timestamp
covid_hosp_prod:
  script: scripts/covid_hosp_prod.R
  store: covid_hosp_prod
  use_crew: yes
  reporter_make: timestamp
covid_hosp_evaluation:
  # Same script as covid_hosp_prod; it dispatches on the project name. Separate
  # store so evaluation runs don't invalidate the production cache.
  script: scripts/covid_hosp_prod.R
  store: covid_hosp_evaluation
  use_crew: yes
  reporter_make: timestamp
rsv_hosp_prod:
  # STUB: scripts/rsv_hosp_prod.R is not written yet (not a priority).
  script: scripts/rsv_hosp_prod.R
  store: rsv_hosp_prod
  use_crew: yes
  reporter_make: timestamp
